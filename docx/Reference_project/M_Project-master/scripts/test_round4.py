"""
Round 4 协议测试脚本
验证：
1. RESET_ERROR 现在能返回 RESPONSE(0x07, OK) 帧
2. READ_STATUS 正常
3. 运动命令（MOVE_REL）的 ACK 不再触发 ERROR
"""
import serial, struct, time, sys

CRC8_TABLE = [
    
]

STATES = ['INIT','IDLE','HOMING','RUNNING','CYCLING','ERROR','ESTOP']
ERRORS = ['NONE','STALL','OVERCURRENT','OVERTEMP','HOME_FAILED','POS_ERROR','ESTOP']
EVT_NAMES = {
    0x80:'ACK_RECV', 0x81:'ACT_DONE', 0x82:'MOTOR_ERROR',
    0x83:'MOTOR_DATA', 0x84:'HOME_START', 0x85:'HOME_DONE',
    0x86:'HOME_FAILED', 0x87:'ESTOP', 0x88:'TIMEOUT'
}

def crc8(data):
    crc = data[0]
    for i in range(1, len(data)):
        crc = CRC8_TABLE[crc ^ data[i]]
    return crc

def build_frame(cmd, data=b""):
    payload = bytes([cmd]) + data
    c = crc8(payload)
    return bytes([0xAA, len(payload)]) + payload + bytes([c, 0x55])

def extract_frames(buf):
    frames = []
    i = 0
    while i < len(buf) - 3:
        if buf[i] == 0xAA:
            plen = buf[i+1]
            end = i + plen + 4
            if end <= len(buf) and buf[end-1] == 0x55:
                payload = buf[i+2:i+2+plen]
                if len(payload) == plen and crc8(payload) == buf[end-2]:
                    frames.append(payload)
                    i = end
                    continue
        i += 1
    return frames

def parse_payload(payload):
    cmd = payload[0]
    if cmd == 0x00 and len(payload) >= 4:
        evt  = payload[1]
        func = payload[2]
        st   = payload[3]
        data = payload[4:] if len(payload) > 4 else b''
        name = EVT_NAMES.get(evt, f'EVT_0x{evt:02X}')
        return f"EVENT {name}(0x{evt:02X}) func=0x{func:02X} status=0x{st:02X} data=[{data.hex(' ')}]"
    else:
        status = payload[1] if len(payload) > 1 else 0
        data   = payload[2:] if len(payload) > 2 else b''
        stat_str = {0:'OK', 1:'FAIL', 2:'BUSY'}.get(status, f'0x{status:02X}')
        extra = ''
        if cmd == 0x10 and len(data) >= 2:
            s = STATES[data[0]] if data[0] < len(STATES) else f'?{data[0]}'
            e = ERRORS[data[1]] if data[1] < len(ERRORS) else f'?{data[1]}'
            extra = f'  → state={s}, error={e}'
        return f"RESPONSE CMD=0x{cmd:02X} {stat_str} data=[{data.hex(' ')}]{extra}"

def send_and_recv(ser, frame, label, wait=1.0):
    print(f"\n{'='*60}")
    print(f">>> SEND {label}: {frame.hex(' ')}")
    ser.reset_input_buffer()
    ser.write(frame)
    time.sleep(wait)
    raw = ser.read(4096)
    if raw:
        frames = extract_frames(raw)
        print(f"<<< 收到 {len(frames)} 帧 (raw {len(raw)} bytes):")
        for p in frames:
            print(f"    [{p.hex(' ')}]  →  {parse_payload(p)}")
    else:
        print("<<< 无响应")
    return raw

PORT = 'COM4'
print(f"连接 {PORT} 115200...")
try:
    ser = serial.Serial(PORT, 115200, timeout=0.5)
except Exception as e:
    print(f"ERROR: {e}")
    sys.exit(1)

time.sleep(0.3)
ser.read(9999)  # drain

# ==== 步骤1：读状态 ====
send_and_recv(ser, build_frame(0x10), "READ_STATUS")

# ==== 步骤2：发 RESET_ERROR ====
# 期望：ACK_RECV 事件 + RESPONSE(0x07, OK)  ← 新增的修复
send_and_recv(ser, build_frame(0x07), "RESET_ERROR", wait=1.0)

# ==== 步骤3：再读状态确认 IDLE ====
send_and_recv(ser, build_frame(0x10), "READ_STATUS (after reset)")

# ==== 步骤4：发 HOME ====
print(f"\n{'='*60}")
print(">>> SEND HOME (等待 5 秒观察 HOME_START / HOME events)...")
ser.reset_input_buffer()
ser.write(build_frame(0x01))
time.sleep(5.0)
raw = ser.read(8192)
frames = extract_frames(raw)
print(f"<<< 收到 {len(frames)} 帧:")
for p in frames:
    print(f"    [{p.hex(' ')}]  →  {parse_payload(p)}")

# ==== 步骤5：读状态 ====
send_and_recv(ser, build_frame(0x10), "READ_STATUS (after HOME attempt)")

# ==== 步骤6：RESET_ERROR 后发 MOVE_REL 90度 ====
send_and_recv(ser, build_frame(0x07), "RESET_ERROR (pre-move)")
time.sleep(0.5)
send_and_recv(ser, build_frame(0x10), "READ_STATUS (pre-move)")

# MOVE_REL: pos=900 (90°), vel=1000 (100RPM), acc=200
print(f"\n{'='*60}")
print(">>> SEND MOVE_REL 90° (等待 10 秒观察 ACK + ACT_DONE)...")
ser.reset_input_buffer()
ser.write(build_frame(0x03, struct.pack('>iHH', 900, 1000, 200)))
time.sleep(10.0)
raw = ser.read(8192)
frames = extract_frames(raw)
print(f"<<< 收到 {len(frames)} 帧:")
for p in frames:
    print(f"    [{p.hex(' ')}]  →  {parse_payload(p)}")

# 最终状态
send_and_recv(ser, build_frame(0x10), "READ_STATUS (final)")

ser.close()
print("\n测试完成")
